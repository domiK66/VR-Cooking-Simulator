using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;

public class CenterIngredients : MonoBehaviour
{
    /*
    public Transform parentObject; // Parent object to which objects will be centered


    private double currentStackHeight = 0.841000021; // Current accumulated stack height



    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Centerable"))
        {
            Debug.Log("test " + parentObject);
            Debug.Log("test " + other);


            // Calculate the stack position based on the current accumulated stack height
            //Vector3 stackPosition = parentObject.position + Vector3.up * currentStackHeight;
       
            //other.rigidbody.isKinetic = true;
           
            /*
            Rigidbody rigidbody = other.GetComponentInParent<Rigidbody>();
            if (rigidbody != null)
            {
                Debug.Log("test" + rigidbody.name);
                rigidbody.isKinematic = true;
            }
            


            other.transform.position = new Vector3((float)-0.0219999999, (float)currentStackHeight, (float)0.488000005);

            other.tag = "Untagged";

            string childName = "HandGrabInteractable"; // Replace with the name of the child GameObject you want to retrieve
            GameObject childObject = other.transform.GetChild(1).gameObject;

            Destroy(childObject);

            /// other.gameObject.AddComponent<Rigidbody>();



            currentStackHeight = currentStackHeight + 0.1;
            // Set the object's position to the calculated stack position
            //  other.transform.position = stackPosition;

            // Increase the current accumulated stack height
            // currentStackHeight += stackHeight;

            // Parent the object to the parent object
            // other.transform.parent = parentObject;
        }
    }

    public void Update()
    {

    }
    */

}